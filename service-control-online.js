(()=>{
'use strict';
const VERSION='2.21.6';
const MANAGER_ROLES=['administrador_general','administrador_academico','encargado_curso'];
const BOLIVIA_CENTER=[-16.2902,-63.5887];
const UNIFORMS=['1-A','1-C','3-A','3-B','8 · Buzo deportivo'];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const session=()=>{try{return JSON.parse(localStorage.getItem('agenda-academic-session')||'null')}catch{return null}};
const manager=()=>MANAGER_ROLES.includes(session()?.role);
const rpc=(fn,body)=>window.academicRPC(fn,body);
let events=[],mounting=false,lastRenderKey='',leafletPromise=null;
function token(){return session()?.session_token}
function fmtDate(v){try{return new Date(v).toLocaleString('es-BO',{dateStyle:'short',timeStyle:'short'})}catch{return v||''}}
function deviceId(){let id=localStorage.getItem('agenda-gps-device');if(!id){id='gps_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,8);localStorage.setItem('agenda-gps-device',id)}return id}
function getPosition(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('Sin geolocalización'));navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,timeout:15000,maximumAge:0})})}
function validPoint(lat,lng){lat=Number(lat);lng=Number(lng);return Number.isFinite(lat)&&Number.isFinite(lng)&&lat>=-90&&lat<=90&&lng>=-180&&lng<=180&&!(lat===0&&lng===0)}
function courseLabel(){return (document.querySelector('#academicCourseSelect option:checked')?.textContent||document.querySelector('.online-module-head .eyebrow')?.textContent||document.querySelector('.academic-course-chip')?.textContent||'Curso').trim()}
function extractMeta(detail=''){const text=String(detail||'');const m=text.match(/(?:^|\n)UNIFORME:\s*([^\n]+)/i);const uniform=m?m[1].trim():'';const clean=text.replace(/(?:^|\n)UNIFORME:\s*[^\n]+/i,'').replace(/^\s+|\s+$/g,'');return{uniform,detail:clean}}
function composeDetail(uniform,detail){return `UNIFORME: ${uniform}${detail?`\n${detail.trim()}`:''}`}
async function load(){if(!token())return[];try{events=await rpc('academic_list_service_events',{p_token:token()})||[];return events}catch(e){console.error(e);return[]}}
function mapId(id){return 'gpsEventMap_'+String(id).replace(/[^a-zA-Z0-9_-]/g,'')}
function renderKey(){return events.map(e=>[e.id,e.updated_at||'',e.total_checkins||0,e.my_checkin?1:0,e.my_status||''].join(':')).join('|')}
function eventCard(e){const checked=e.my_checkin,lat=Number(e.latitude),lng=Number(e.longitude),hasPoint=validPoint(lat,lng),meta=extractMeta(e.detail),radius=Number(e.radius_m)||100;return `<article class="gps-event ${checked?'checked':''}" data-event-id="${esc(e.id)}"><div class="gps-event-head"><div><small class="gps-course-line">${esc(courseLabel())}</small><b>Formación</b><small>${fmtDate(e.starts_at)}</small></div><span>${e.total_inside||0}/${e.total_checkins||0}</span></div>${meta.uniform?`<div class="gps-uniform-pill">🎽 Uniforme ${esc(meta.uniform)}</div>`:''}${meta.detail?`<p>${esc(meta.detail)}</p>`:''}<div class="gps-facts"><span>📍 Radio ${radius} m</span>${checked?`<span>✓ ${esc(e.my_status)} · ${Math.round(e.my_distance_m||0)} m</span>`:'<span>Pendiente de confirmar</span>'}</div>${hasPoint?`<div class="gps-event-map-wrap"><div class="gps-event-map" id="${mapId(e.id)}" data-lat="${lat}" data-lng="${lng}" data-radius="${radius}"></div><div class="gps-map-caption"><span>📍 Punto exacto de formación</span><small>${lat.toFixed(6)}, ${lng.toFixed(6)}</small></div></div>`:'<div class="gps-map-missing">Ubicación no disponible para esta formación.</div>'}${!checked?'<p class="gps-reader-help">Cuando llegue al punto indicado, active la ubicación del teléfono y confirme su presencia.</p>':''}<div class="gps-actions">${checked?'<button disabled>✓ Ubicación confirmada</button>':`<button data-gps-checkin="${e.id}">📍 Confirmar mi ubicación en el punto de formación</button>`}${hasPoint?`<a target="_blank" rel="noopener" href="https://www.google.com/maps?q=${lat},${lng}">Abrir en Google Maps</a>`:''}${manager()?`<button class="gps-delete" data-gps-delete="${e.id}" type="button">Eliminar</button>`:''}</div></article>`}
function render(){return `<section class="gps-control"><div class="gps-toolbar gps-toolbar-v2216"><div><span>${esc(courseLabel())}</span><b>Formaciones GPS</b></div>${manager()?'<button id="gpsNewService">＋ Nueva formación GPS</button>':''}</div><div id="gpsEvents">${events.map(eventCard).join('')||'<div class="gps-empty">No hay formaciones GPS activas para este paralelo.</div>'}</div></section>`}
function ensureLeaflet(){if(window.L)return Promise.resolve(window.L);if(leafletPromise)return leafletPromise;leafletPromise=new Promise((resolve,reject)=>{let css=document.querySelector('link[data-agenda-leaflet]');if(!css){css=document.createElement('link');css.rel='stylesheet';css.dataset.agendaLeaflet='1';css.href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';document.head.appendChild(css)}let s=document.querySelector('script[data-agenda-leaflet]');if(s){if(window.L)return resolve(window.L);s.addEventListener('load',()=>resolve(window.L),{once:true});s.addEventListener('error',reject,{once:true});return}s=document.createElement('script');s.dataset.agendaLeaflet='1';s.src='https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';s.onload=()=>resolve(window.L);s.onerror=reject;document.head.appendChild(s)});return leafletPromise}
async function initEventMaps(root=document){try{await ensureLeaflet()}catch{return}root.querySelectorAll('.gps-event-map').forEach(el=>{if(el.dataset.mapReady==='1')return;const lat=Number(el.dataset.lat),lng=Number(el.dataset.lng),radius=Number(el.dataset.radius)||100;if(!validPoint(lat,lng))return;el.dataset.mapReady='1';const map=L.map(el,{zoomControl:true,attributionControl:false,scrollWheelZoom:false,fadeAnimation:false,zoomAnimation:false,markerZoomAnimation:false}).setView([lat,lng],16);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,updateWhenIdle:true,keepBuffer:4}).addTo(map);L.marker([lat,lng]).addTo(map);L.circle([lat,lng],{radius,weight:2,fillOpacity:.08}).addTo(map);requestAnimationFrame(()=>map.invalidateSize(false))})}
async function refresh(container,force=false){await load();if(!container)return;const key=renderKey();if(force||key!==lastRenderKey||!container.querySelector('#gpsEvents')){container.innerHTML=render();lastRenderKey=key;bind(container)}initEventMaps(container)}
function bind(root=document){root?.querySelector('#gpsNewService')?.addEventListener('click',openCreate);root?.querySelectorAll('[data-gps-checkin]').forEach(b=>b.onclick=()=>checkin(b.dataset.gpsCheckin,root));root?.querySelectorAll('[data-gps-delete]').forEach(b=>b.onclick=()=>deleteEvent(b.dataset.gpsDelete,root))}
async function checkin(id,root){const btn=root.querySelector(`[data-gps-checkin="${id}"]`);if(btn){btn.disabled=true;btn.textContent='Verificando ubicación…'}try{const p=await getPosition(),c=p.coords,r=await rpc('academic_service_checkin',{p_token:token(),p_event_id:id,p_latitude:c.latitude,p_longitude:c.longitude,p_accuracy_m:c.accuracy||null,p_device_id:deviceId()}),x=Array.isArray(r)?r[0]:r;window.toast?.(x?.within_radius?'Presencia confirmada dentro del punto de formación':'Ubicación registrada, pero está fuera del radio permitido');lastRenderKey='';await refresh(root,true)}catch(e){console.error(e);window.toast?.('No se pudo verificar su ubicación');if(btn){btn.disabled=false;btn.textContent='📍 Confirmar mi ubicación en el punto de formación'}}}
async function deleteEvent(id,root){if(!manager())return;if(!confirm('¿Eliminar esta formación GPS? Esta acción quitará la formación de este paralelo.'))return;const btn=root.querySelector(`[data-gps-delete="${id}"]`);if(btn){btn.disabled=true;btn.textContent='Eliminando…'}try{await rpc('academic_delete_service_event',{p_token:token(),p_event_id:id});window.toast?.('Formación eliminada');lastRenderKey='';await refresh(root,true)}catch(e){console.error(e);window.toast?.('No se pudo eliminar la formación. La función de borrado del servidor debe estar habilitada.');if(btn){btn.disabled=false;btn.textContent='Eliminar'}}}
function parseMapText(value){const s=String(value||'').trim();let m=s.match(/@(-?\d{1,2}(?:\.\d+)?),(-?\d{1,3}(?:\.\d+)?)/);if(!m)m=s.match(/[?&](?:q|query|ll)=(-?\d{1,2}(?:\.\d+)?)[,%2C\s]+(-?\d{1,3}(?:\.\d+)?)/i);if(!m)m=s.match(/^\s*(-?\d{1,2}(?:\.\d+)?)\s*[,;]\s*(-?\d{1,3}(?:\.\d+)?)\s*$/);if(!m)return null;const lat=Number(m[1]),lng=Number(m[2]);return validPoint(lat,lng)?{lat,lng}:null}
async function searchPlace(query){const q=String(query||'').trim();if(!q)return[];const url='https://nominatim.openstreetmap.org/search?format=jsonv2&limit=5&countrycodes=bo&q='+encodeURIComponent(q);const r=await fetch(url,{headers:{'Accept-Language':'es'}});if(!r.ok)throw new Error('search');return (await r.json()).filter(x=>validPoint(x.lat,x.lon)).map(x=>({lat:Number(x.lat),lng:Number(x.lon),label:x.display_name||q}))}
async function openCreate(){if(!manager())return window.toast?.('No tiene permiso para crear una formación GPS');const now=new Date(Date.now()+10*60000),local=new Date(now.getTime()-now.getTimezoneOffset()*60000).toISOString().slice(0,16),last=(()=>{try{return JSON.parse(localStorage.getItem('agenda-last-formation-point')||'null')}catch{return null}})(),initial=validPoint(last?.lat,last?.lng)?[Number(last.lat),Number(last.lng)]:BOLIVIA_CENTER;const host=document.createElement('div');host.className='gps-sheet';host.innerHTML=`<div class="gps-sheet-card gps-sheet-map-card"><div class="gps-sheet-head"><div><span>${esc(courseLabel())}</span><h3>Nueva formación GPS</h3></div><button id="gpsCancel" aria-label="Cerrar">×</button></div><p class="gps-create-help">Defina el lugar exacto. Ningún cambio se guarda hasta tocar <b>Confirmar y crear</b>.</p><label>Uniforme<select id="gpsUniform">${UNIFORMS.map((u,i)=>`<option value="${esc(u)}" ${i===1?'selected':''}>${esc(u)}</option>`).join('')}</select></label><label>Fecha y hora de control<input id="gpsStart" type="datetime-local" value="${local}"></label><section class="gps-location-picker"><div class="gps-location-title"><b>Ubicación de la formación</b><small>Toque el mapa o mueva el pin.</small></div><div class="gps-search-row"><input id="gpsPlaceSearch" placeholder="Buscar lugar, calle o institución"><button id="gpsSearchBtn" type="button">Buscar</button></div><div id="gpsSearchResults" class="gps-search-results"></div><div class="gps-link-row"><input id="gpsMapLink" placeholder="Pegar enlace de Google Maps o latitud,longitud"><button id="gpsUseLink" type="button">Usar</button></div><div class="gps-map-tools"><button id="gpsMyLocation" type="button">⌖ Usar mi ubicación actual</button><span id="gpsPointText">Seleccione un punto</span></div><div id="gpsCreateMap" class="gps-create-map"></div></section><label>Radio de confirmación<input id="gpsRadius" type="number" min="20" max="5000" value="100"><small>Predeterminado: 100 metros</small></label><label>Comunicado / detalle<textarea id="gpsDetail" rows="2" placeholder="Detalle breve de la formación"></textarea></label><div class="gps-confirm-note">Antes de publicar, revise hora, uniforme, punto del mapa y radio. La formación corresponde únicamente al paralelo actual.</div><div class="gps-sheet-actions"><button id="gpsCancelBottom" type="button">Cancelar</button><button id="gpsSave" type="button">Confirmar y crear</button></div></div>`;document.body.appendChild(host);const close=()=>{try{host._agendaMap?.remove()}catch{}host.remove()};host.querySelector('#gpsCancel').onclick=close;host.querySelector('#gpsCancelBottom').onclick=close;let selected={lat:initial[0],lng:initial[1]},marker=null,circle=null,map=null;function updatePoint(lat,lng,zoom=true,label=''){if(!validPoint(lat,lng))return;selected={lat:Number(lat),lng:Number(lng)};marker?.setLatLng([selected.lat,selected.lng]);circle?.setLatLng([selected.lat,selected.lng]);if(zoom)map?.setView([selected.lat,selected.lng],16);const t=host.querySelector('#gpsPointText');if(t)t.textContent=label||`${selected.lat.toFixed(6)}, ${selected.lng.toFixed(6)}`}
try{await ensureLeaflet();map=L.map(host.querySelector('#gpsCreateMap'),{zoomControl:true,attributionControl:false,fadeAnimation:false,zoomAnimation:false,markerZoomAnimation:false}).setView(initial,validPoint(last?.lat,last?.lng)?16:5);host._agendaMap=map;L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,updateWhenIdle:true,keepBuffer:4}).addTo(map);marker=L.marker(initial,{draggable:true}).addTo(map);circle=L.circle(initial,{radius:100,weight:2,fillOpacity:.08}).addTo(map);marker.on('dragend',e=>{const p=e.target.getLatLng();updatePoint(p.lat,p.lng,false)});map.on('click',e=>updatePoint(e.latlng.lat,e.latlng.lng,false));requestAnimationFrame(()=>map.invalidateSize(false));updatePoint(initial[0],initial[1],false,validPoint(last?.lat,last?.lng)?'Último punto utilizado':'Seleccione el punto en el mapa')}catch{host.querySelector('#gpsCreateMap').innerHTML='<div class="gps-map-error">No se pudo cargar el mapa. Puede pegar coordenadas de Google Maps.</div>'}
host.querySelector('#gpsRadius').oninput=e=>circle?.setRadius(Number(e.target.value)||100);host.querySelector('#gpsMyLocation').onclick=async()=>{const b=host.querySelector('#gpsMyLocation');b.disabled=true;b.textContent='Buscando…';try{const p=await getPosition();updatePoint(p.coords.latitude,p.coords.longitude,true,'Mi ubicación actual')}catch{window.toast?.('No se pudo obtener su ubicación')}finally{b.disabled=false;b.textContent='⌖ Usar mi ubicación actual'}};host.querySelector('#gpsUseLink').onclick=()=>{const p=parseMapText(host.querySelector('#gpsMapLink').value);if(!p)return window.toast?.('Ese enlace no contiene coordenadas visibles. Seleccione el punto en el mapa o pegue latitud,longitud.');updatePoint(p.lat,p.lng,true,'Punto tomado del enlace')};host.querySelector('#gpsSearchBtn').onclick=async()=>{const q=host.querySelector('#gpsPlaceSearch').value,box=host.querySelector('#gpsSearchResults');box.innerHTML='<span>Buscando…</span>';try{const rows=await searchPlace(q);box.innerHTML=rows.length?rows.map((r,i)=>`<button type="button" data-gps-result="${i}">${esc(r.label)}</button>`).join(''):'<span>No se encontraron lugares.</span>';box.querySelectorAll('[data-gps-result]').forEach(b=>b.onclick=()=>{const r=rows[Number(b.dataset.gpsResult)];updatePoint(r.lat,r.lng,true,r.label);box.innerHTML=''})}catch{box.innerHTML='<span>No se pudo buscar ahora.</span>'}};host.querySelector('#gpsSave').onclick=async()=>{if(!validPoint(selected.lat,selected.lng))return window.toast?.('Seleccione la ubicación de la formación');const uniform=host.querySelector('#gpsUniform').value,start=host.querySelector('#gpsStart').value,radius=Number(host.querySelector('#gpsRadius').value)||100,detail=host.querySelector('#gpsDetail').value.trim();if(!start)return window.toast?.('Seleccione fecha y hora');const summary=`${courseLabel()}\n${new Date(start).toLocaleString('es-BO')}\nUniforme: ${uniform}\nRadio: ${radius} m\n\n¿Confirmar esta formación GPS?`;if(!confirm(summary))return;const b=host.querySelector('#gpsSave');b.disabled=true;b.textContent='Publicando…';try{await rpc('academic_create_service_event',{p_token:token(),p_title:'Formación',p_service_type:'predeterminado',p_detail:composeDetail(uniform,detail),p_starts_at:new Date(start).toISOString(),p_ends_at:null,p_latitude:selected.lat,p_longitude:selected.lng,p_radius_m:radius,p_checkin_before_min:30,p_checkin_after_min:30});localStorage.setItem('agenda-last-formation-point',JSON.stringify(selected));close();const root=document.querySelector('[data-gps-control-root]');lastRenderKey='';if(root)refresh(root,true);window.toast?.('Formación GPS publicada')}catch(e){console.error(e);b.disabled=false;b.textContent='Confirmar y crear';window.toast?.('No se pudo crear la formación GPS')}}}
function isFormacionesOnline(){const main=document.querySelector('main');return!!token()&&!!main?.querySelector('.online-profile')&&([...main.querySelectorAll('.academic-subnav button.active,.academic-text-nav button.active')].some(b=>/formaciones/i.test(b.textContent||''))||[...main.querySelectorAll('.online-module-head h3')].some(h=>/^formaciones$/i.test((h.textContent||'').trim())))}
function isOnlinePanel(){const main=document.querySelector('main');if(!token()||!main?.querySelector('.online-profile'))return false;return !!main.querySelector('.academic-welcome,.academic-summary-grid,.academic-module-grid')&&!isFormacionesOnline()}
function panelButtonHtml(){return `<span class="gps-panel-icon">⌖</span><span class="gps-panel-copy"><small>${esc(courseLabel())}</small><b>${manager()?'Nueva formación':'Formación'} GPS</b><em>Mapa · hora · uniforme · ubicación</em></span><span class="gps-panel-arrow">›</span>`}
function installPanelAccess(){const old=document.getElementById('gpsFormationPanelAccess');if(!isOnlinePanel()){old?.remove();return}if(old)return;const main=document.querySelector('main');if(!main)return;const card=document.createElement('button');card.id='gpsFormationPanelAccess';card.type='button';card.className='gps-panel-access';card.innerHTML=panelButtonHtml();card.onclick=openGpsFormationFromPanel;const modules=main.querySelector('.academic-module-grid,.academic-summary-grid,.academic-visual-modules');(modules||main.lastElementChild)?.insertAdjacentElement('afterend',card)}
function openGpsFormationFromPanel(){if(typeof window.setAcademicTab==='function')window.setAcademicTab('formaciones');else if(typeof academicTab!=='undefined'){academicTab='formaciones';window.render?.()}setTimeout(()=>{autoMount();if(manager())setTimeout(openCreate,220)},300)}
async function autoMount(){if(mounting)return;installPanelAccess();const old=document.querySelector('[data-gps-control-root]');if(!isFormacionesOnline()){old?.remove();return}const main=document.querySelector('main');let root=old;if(!root){root=document.createElement('div');root.dataset.gpsControlRoot='1';const head=main.querySelector('.online-module-head');if(head)head.insertAdjacentElement('afterend',root);else return}if(root.dataset.mounted==='1'){initEventMaps(root);return}mounting=true;try{await refresh(root,true);root.dataset.mounted='1'}finally{mounting=false}}
window.AgendaGPSServiceControl={load,render,bind,refresh,openCreate,checkin,deleteEvent,openGpsFormationFromPanel,version:VERSION};
window.addEventListener('DOMContentLoaded',()=>{setTimeout(()=>{installPanelAccess();autoMount()},450);let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;installPanelAccess();autoMount()})}).observe(document.getElementById('app')||document.body,{childList:true,subtree:true})});
})();
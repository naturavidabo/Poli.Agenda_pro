(()=>{
'use strict';
const VERSION='2.21.8';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const session=()=>{try{return JSON.parse(localStorage.getItem('agenda-academic-session')||'null')}catch{return null}};
const token=()=>session()?.session_token;
const rpc=(fn,body)=>window.academicRPC(fn,body);
const cache=new Map();
function label(status){return ({presente:'Presente',fuera_radio:'Fuera de radio',temprano:'Temprano',tarde:'Tarde',sin_marcar:'Sin marcar'})[status]||status||'Sin marcar'}
function group(status){if(status==='presente')return 'present';if(status==='sin_marcar')return 'missing';return 'observed'}
function fmtTime(v){if(!v)return '';try{return new Date(v).toLocaleTimeString('es-BO',{hour:'2-digit',minute:'2-digit'})}catch{return ''}}
function injectStyles(){if(document.getElementById('gpsRosterStyles'))return;const s=document.createElement('style');s.id='gpsRosterStyles';s.textContent=`
.gps-roster-toggle{width:100%;margin-top:10px;border:1px solid #b9c9b6;background:#f7faf5;color:#164d31;border-radius:13px;padding:11px 14px;font-weight:800;display:flex;align-items:center;justify-content:space-between;gap:8px}
.gps-roster-toggle span{font-weight:600;color:#607066;font-size:12px}.gps-roster{margin-top:10px;border:1px solid #dbe5d9;background:#fff;border-radius:16px;overflow:hidden}.gps-roster[hidden]{display:none}.gps-roster-loading,.gps-roster-error{padding:16px;text-align:center;color:#66756b}.gps-roster-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;padding:10px;background:#f5f8f2;border-bottom:1px solid #e2e9df}.gps-roster-stat{padding:8px 5px;text-align:center;border-radius:10px;background:#fff;border:1px solid #e4e9e1}.gps-roster-stat b{display:block;font-size:18px;color:#174c31}.gps-roster-stat small{font-size:10px;color:#657168}.gps-roster-filters{display:flex;gap:6px;padding:9px;overflow:auto;border-bottom:1px solid #edf0eb}.gps-roster-filter{white-space:nowrap;border:1px solid #ccd7c8;background:#fff;color:#355441;border-radius:999px;padding:7px 10px;font-size:11px;font-weight:700}.gps-roster-filter.active{background:#245c36;color:#fff;border-color:#245c36}.gps-roster-list{max-height:380px;overflow:auto}.gps-roster-row{display:grid;grid-template-columns:36px 1fr auto;gap:9px;align-items:center;padding:10px 12px;border-bottom:1px solid #eef1ec}.gps-roster-row:last-child{border-bottom:0}.gps-roster-number{width:32px;height:32px;border-radius:10px;background:#eef4eb;display:flex;align-items:center;justify-content:center;font-weight:800;color:#36553f}.gps-roster-name{min-width:0}.gps-roster-name b{display:block;font-size:12px;color:#1c2d22;line-height:1.25}.gps-roster-name small{display:block;font-size:10px;color:#7a857d;margin-top:2px}.gps-roster-status{text-align:right}.gps-roster-pill{display:inline-block;border-radius:999px;padding:5px 8px;font-size:10px;font-weight:800}.gps-roster-pill.present{background:#dff1df;color:#17602c}.gps-roster-pill.missing{background:#f1f1ed;color:#62665f}.gps-roster-pill.observed{background:#fff0c9;color:#7a5410}.gps-roster-time{display:block;font-size:9px;color:#89918b;margin-top:3px}
`;document.head.appendChild(s)}
async function loadRoster(eventId,box){box.innerHTML='<div class="gps-roster-loading">Cargando nómina del paralelo…</div>';try{let rows=cache.get(eventId);if(!rows){rows=await rpc('academic_service_event_roster',{p_token:token(),p_event_id:eventId})||[];cache.set(eventId,rows)}renderRoster(box,rows)}catch(e){console.error(e);box.innerHTML='<div class="gps-roster-error">No se pudo cargar la nómina de esta formación.</div>'}}
function renderRoster(box,rows){const total=rows.length,present=rows.filter(x=>x.status==='presente').length,missing=rows.filter(x=>x.status==='sin_marcar').length,observed=total-present-missing;box.innerHTML=`<div class="gps-roster-summary"><div class="gps-roster-stat"><b>${total}</b><small>Total</small></div><div class="gps-roster-stat"><b>${present}</b><small>Presentes</small></div><div class="gps-roster-stat"><b>${missing}</b><small>Sin marcar</small></div></div><div class="gps-roster-filters"><button class="gps-roster-filter active" data-filter="all">Todos ${total}</button><button class="gps-roster-filter" data-filter="present">Presentes ${present}</button><button class="gps-roster-filter" data-filter="missing">Sin marcar ${missing}</button><button class="gps-roster-filter" data-filter="observed">Novedades ${observed}</button></div><div class="gps-roster-list">${rows.map(r=>`<div class="gps-roster-row" data-group="${group(r.status)}"><div class="gps-roster-number">${r.roster_number??'—'}</div><div class="gps-roster-name"><b>${esc(r.full_name)}</b>${r.department?`<small>${esc(r.department)}</small>`:''}</div><div class="gps-roster-status"><span class="gps-roster-pill ${group(r.status)}">${esc(label(r.status))}</span>${r.checked_at?`<small class="gps-roster-time">${fmtTime(r.checked_at)}${r.distance_m!=null?' · '+Math.round(r.distance_m)+' m':''}</small>`:''}</div></div>`).join('')||'<div class="gps-roster-loading">No hay integrantes activos en este paralelo.</div>'}</div>`;box.querySelectorAll('.gps-roster-filter').forEach(b=>b.onclick=()=>{box.querySelectorAll('.gps-roster-filter').forEach(x=>x.classList.toggle('active',x===b));const f=b.dataset.filter;box.querySelectorAll('.gps-roster-row').forEach(r=>r.hidden=f!=='all'&&r.dataset.group!==f)})}
function enhanceCard(card){if(card.dataset.rosterEnhanced==='1')return;const eventId=card.dataset.eventId;if(!eventId)return;card.dataset.rosterEnhanced='1';const actions=card.querySelector('.gps-actions');if(!actions)return;const toggle=document.createElement('button');toggle.type='button';toggle.className='gps-roster-toggle';toggle.innerHTML='<b>👥 Ver asistencia del paralelo</b><span>Presentes y sin marcar ▾</span>';const box=document.createElement('div');box.className='gps-roster';box.hidden=true;actions.insertAdjacentElement('afterend',toggle);toggle.insertAdjacentElement('afterend',box);toggle.onclick=async()=>{box.hidden=!box.hidden;toggle.querySelector('span').textContent=box.hidden?'Presentes y sin marcar ▾':'Ocultar lista ▴';if(!box.hidden&&!box.dataset.loaded){box.dataset.loaded='1';await loadRoster(eventId,box)}}}
function scan(){injectStyles();document.querySelectorAll('.gps-event[data-event-id]').forEach(enhanceCard)}
window.AgendaServiceRoster={scan,version:VERSION,clearCache:()=>cache.clear()};
window.addEventListener('DOMContentLoaded',()=>{setTimeout(scan,700);let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;scan()})}).observe(document.getElementById('app')||document.body,{childList:true,subtree:true})});
})();